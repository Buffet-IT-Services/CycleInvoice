"""App configuration for the API module of the Cycle Invoice application."""
from django.apps import AppConfig


class ApiConfig(AppConfig):
    """Configuration class for the Cycle Invoice API application."""

    name = "api"
