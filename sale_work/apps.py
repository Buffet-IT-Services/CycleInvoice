"""Application configuration for the sale_work app."""

from django.apps import AppConfig


class SaleWorkConfig(AppConfig):
    """Configuration for the sale_work app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "sale_work"
